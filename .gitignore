# Big GRFs (distribute separately)
data.grf
en.grf

# HD content (downloaded on-demand from R2 by the launcher)
hd.grf
hd-*.grf
hd-manifest.json

# Local dev launcher HTML (production fetches from GitHub Pages)
launcher.html

# Logs (debug runs, R2 sync, etc)
*.log

# Local dev server GRF (contains local-only assets, distribute separately)
server-local.grf

# Backup/scratch
_loose_backup/
*.epi
*.lock
*.secure.txt
*.bak
!DATA-vanilla.ini.bak
!DATA-hd.ini.bak
*.bak-*
*.before-*
Ragexe.exe.unpatched
Ragexe.exe.pre-warp-bak
Setup.exe.kr-bak
Launcher.dat
DimensionsRO.dat
en.grf.bak
data.grf.original-*

# Test launcher artifacts
LauncherTest.*
launcher_test_*

# ReShade (not part of distribution)
ReShade.ini
ReShade*

# Multipart split state files
*.state.json

# Sprite test/work GRFs (regenerate via dev tooling)
sprite-test*.grf
head*-test.grf
hud-skin-test.grf
mood-test.grf
mood-prt.grf

# Loose data folder (extracted from GRFs for dev iteration; rebuild GRF to distribute)
data/

# Installer build artifacts (large, distribute via GitHub releases)
installer/payload.7z
installer/DimensionsRO-Setup.exe

# State (savedata is per-player, ignored — except the default-skin template
# below which seeds a fresh install with dimensionsro_dark active)
savedata/*
!savedata/OptionInfo.lua
ScreenShot/
_tmpEmblem/
GameGuard/
PatchClient/
Replay/
memo/

# HD texture pack and mood pack (distribute via gofile)
textures-hd.grf
textures-hd-2.grf
textures-hd-3.grf
textures-hd-4.grf
mood.grf
skybox-hd.grf

# DimensionsRO dark skin (~1.6 GB, ~25k BMPs).
# Ships to players via thor patch (rpatchur). Regenerate locally with
# tools/skin-recolor/ (extract from data.grf, recolor, overlay en.grf).
Skin/dimensionsro_dark/
